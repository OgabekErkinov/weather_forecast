import { useState } from 'react'
import privateApi from './apis/config'
import { endpoints } from './apis/endpoints'

function App() {
  const [count, setCount] = useState(0)

  getData = async privateApi.get(endpoints.)

  return (
   <></>
  )
}

export default App
